# idea-forge environment config
# Copy this to .env and fill in values

# Required for `forge ideas` and `forge plan`
ANTHROPIC_API_KEY=sk-ant-...

# Optional: raises GitHub rate limit from 60 to 5000 req/hr
# Needed if you're hitting rate limit errors on `forge trending`
GITHUB_TOKEN=ghp_...
